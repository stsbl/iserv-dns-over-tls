\$TTL	3600
@	IN	SOA	${Servername:-$(hostname)}. hostmaster.$Domain. (
			      1		; Serial
			 604800		; Refresh
			  86400		; Retry
			2419200		; Expire
			 604800 )	; Negative Cache TTL
;
@	IN	NS	${Servername:-$(hostname)}.

