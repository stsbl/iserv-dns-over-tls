#!/bin/sh
set -e 

if [ "$1" = "remove" ] || [ "$1" = "purge" ]
then
  rm -f /etc/stunnel/iserv-dns-over-tls.conf
  if ! [ /etc/stunnel/iserv-dns-over-tls.conf* = "/etc/stunnel/iserv-dns-over-tls.conf*" ]
  then
    rm -f /etc/stunnel/iserv-dns-over-tls.conf*
  fi
fi

#DEBHELPER#
